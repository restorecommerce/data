{
  "options": {
    "base": "./data/",
    "concurrency": 1
  },
  "tasks": [
    {
      "name": "products",
      "operation": "sync",
      "src": "./data/",
      "filter": "products.yaml",
      "batchSize": 500,
      "mutation": "mutation mutateProducts($var: [IIoRestorecommerceProductProduct!]!) { catalog { product { Mutate( input: { items: $var, totalCount: 0, mode: CREATE } ) { details{items {status {id, code, message}},  operationStatus { code message }} } } }}"
    }
  ]
}
