{
  "resource_list": [
    {
      "entity": "role"
    },
    {
      "entity": "user"
    }
  ],
  "mutation": "mutation{deleteResources(input:{listOfResources: ${resource_list} }) {deleteStatus, error{code, message} }}"
}
